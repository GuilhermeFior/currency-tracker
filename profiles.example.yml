default:
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: YOUR_GCP_PROJECT
      dataset: fx_dev
      keyfile: {{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}
      location: US
      threads: 4
  target: dev